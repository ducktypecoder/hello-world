module.exports = function submitAnswer(answer) {
  console.log('submitting answer to the api: ', answer);

  return new Promise(function(resolve, reject) {
    setTimeout(function() {
      resolve({ answer: answer, correct: answer });
    }, 1000);
  });
};
